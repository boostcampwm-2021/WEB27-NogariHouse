import styled from 'styled-components';
import { whiteScroll } from '@styles/scrollbar-style';

const NewChatRoomBody = styled.div`
  height: calc(100% - 140px);
  ${whiteScroll};
`;

export default NewChatRoomBody;
