import styled from 'styled-components';

import { BackgroundWrapper } from '@common/modal';

const CustomBackgroundWrapper = styled(BackgroundWrapper)`
  opacity: 0.4;
`;

export default CustomBackgroundWrapper;
